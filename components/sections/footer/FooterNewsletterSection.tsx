export function FooterNewsletterSection() {
  return (
    <footer id="footer" className="min-h-[40vh] bg-[#0A0A0A] border-t border-white/10">
      {/* Rebuild in dedicated slice: footer + newsletter capture flow. */}
    </footer>
  );
}
